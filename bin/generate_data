#!/usr/bin/env python

import sys
import os

# add ../lib to the load path to find training code
sys.path.append(os.path.join(os.path.dirname(__file__), "../lib"))
from training import cli

cli.Cli(sys.argv[1:]).run()
